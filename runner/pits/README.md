# PIT Configurations

This directory contains PIT (Programming Interactive Task) configuration files
used by the runner service at runtime.

Each file defines how tests are executed for a specific PIT.

For full documentation about PIT configuration, structure and examples,
see: /docs/runner.md